name: Unity Build

on:
  push:
    branches:
      - main  # Change cette ligne si ta branche principale a un autre nom

jobs:
  build:
    name: Build Unity Project
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Cache Library
        uses: actions/cache@v3
        with:
          path: Library
          key: Library-${{ runner.os }}-${{ github.sha }}
          restore-keys: |
            Library-${{ runner.os }}-

      - name: Setup Unity
        uses: game-ci/unity-builder@v3
        with:
          unityVersion: 6000.0.32f1
          targetPlatform: StandaloneWindows64  # Modifie selon la plateforme de build

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: UnityBuild
          path: build  # Modifie selon ton chemin de sortie

      - name: Deploy to `builds` branch
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin builds || git branch builds
          git checkout builds
          rm -rf *
          mv build/* .
          git add .
          git commit -m "Auto-built from commit ${{ github.sha }}"
          git push origin builds --force
